- name: install packages
  hosts: local
  connection: local
  tasks:
    - name: install package
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loops:
        - mysql
        - nginx
        - zip
