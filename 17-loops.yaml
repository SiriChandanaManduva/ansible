- name: demo on loops
  hosts: local
  connection: local
  tasks:
    - name: print names
      debug:
        msg: "Hello {{ item }}" #item is reserved keyword
      loop:
        - siri
        - karthik
        - rm
